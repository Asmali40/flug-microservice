meta {
  name: Token admin mit falschem Passwort
  type: http
  seq: 3
}

post {
  url: {{tokenUrl}}
  body: json
  auth: inherit
}

body:json {
  {
      "username": "admin",
      "password": "FALSCHES PASSWORT"
  }
}

assert {
  res.status: eq 401
}

settings {
  encodeUrl: true
  timeout: 0
}
